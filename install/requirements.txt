# Runtime dependencies of the Meridian Loom sidecar (Python >= 3.11).
#
# Kept identical to core/pyproject.toml [project].dependencies and to
# extension/src/interpreter.ts RUNTIME_DEPENDENCIES; core/tests/test_installers.py
# fails if any of the three drift.
#
# These pin the DIRECT dependencies. If install/requirements.lock exists (a
# hash-locked closure of every transitive package, produced by the release
# process), the installers use that instead and pass --require-hashes.
cryptography==49.0.0
tree-sitter==0.25.1
tree-sitter-java==0.23.5
tree-sitter-python==0.25.0
PyYAML==6.0.2
jsonschema==4.26.0
langgraph==1.0.8
langgraph-checkpoint-sqlite==3.1.1
