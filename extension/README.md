# Meridian Loom

A governed organisation of AI agents inside VS Code. This extension is the
host-side shell: activation, views, commands, credentials, progress and the
lifecycle of the sidecar process that runs all orchestration.

See the repository `docs/spec/` for the normative requirements.
