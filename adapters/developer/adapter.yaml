# Developer — prebuilt Meridian adapter (§6.10 roster, M31).
# Governance facet of the adapter manifest (§7.9). ACP transport is
# host-side; this folder carries policy, skills, instructions, learning.
id: developer
version: "1.0.0"
role: developer
acp: true
bridge: null
permittedTools: ["repo_read", "build", "test", "apply_patch"]
trainable:
  policy: false
  rules: true
  memory: true
  skills: false
  calibration: true
actionClasses:
  plan: "assisted"
  review: "assisted"
  diff: "assisted"
  test_run: "deterministic"
  note: "deterministic"
  parse: "deterministic"
  resolve_symbol: "deterministic"
dependencies: ["tech-lead"]
digest: "sha256:c117600b7764982ab61f73f5e2954ddafe39d274137eaa3241a806e6a8642e99"
