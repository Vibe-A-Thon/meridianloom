# Security — prebuilt Meridian adapter (§6.10 roster, M31).
# Governance facet of the adapter manifest (§7.9). ACP transport is
# host-side; this folder carries policy, skills, instructions, learning.
id: security
version: "1.0.0"
role: security
acp: true
bridge: null
permittedTools: ["repo_read", "scan", "static_analysis"]
trainable:
  policy: false
  rules: true
  memory: true
  skills: false
  calibration: true
actionClasses:
  plan: "assisted"
  review: "assisted"
  diff: "assisted"
  test_run: "deterministic"
  note: "deterministic"
  scan: "deterministic"
dependencies: ["architect"]
digest: "sha256:da7fca05fba9723affb3ac8fc62fe739f81ee3b8a1ff78e8502f47d078d05924"
