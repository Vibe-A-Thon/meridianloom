{
  "name": "meridian-loom-root",
  "private": true,
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "build": "node scripts/generate-bus-types.mjs && npm run build --workspace=extension",
    "check:contracts": "node scripts/generate-bus-types.mjs --check",
    "package": "node scripts/package-extension.mjs"
  }
}
